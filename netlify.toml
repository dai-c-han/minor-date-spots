[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"

# SPA フォールバック
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
